## battle simulator v1.0ish

Done under the gun late at night and featuring some questionable design elements as time ticked on

# to play

`composer install` to fetch dependencies

`php artisan battle` to battle
